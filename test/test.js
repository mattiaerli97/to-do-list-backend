import request from "supertest";
import app from "../index.js";

describe("GET /", () => {
    it("First test", (done) => {
      done();
        // request(app).get("/").expect("Hello World!", done);
    })
});
